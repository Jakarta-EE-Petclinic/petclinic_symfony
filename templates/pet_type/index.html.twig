{% extends 'base.html.twig' %}

{% block title %}PetType index{% endblock %}

{% block body %}
    <h1>PetType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pet_type in pet_types %}
            <tr>
                <td>{{ pet_type.id }}</td>
                <td>{{ pet_type.name }}</td>
                <td>
                    <a href="{{ path('app_pet_type_show', {'id': pet_type.id}) }}">show</a>
                    <a href="{{ path('app_pet_type_edit', {'id': pet_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_pet_type_new') }}">Create new</a>
{% endblock %}
