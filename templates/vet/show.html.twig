{% extends 'base.html.twig' %}

{% block title %}Vet{% endblock %}

{% block body %}
    <h1>Vet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vet.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ vet.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ vet.lastName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vet_index') }}">back to list</a>

    <a href="{{ path('app_vet_edit', {'id': vet.id}) }}">edit</a>

    {{ include('vet/_delete_form.html.twig') }}
{% endblock %}
