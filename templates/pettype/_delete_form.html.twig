<form method="post" action="{{ path('app_pettype_delete', {'id': pettype.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pettype.id) }}">
    <button class="btn">Delete</button>
</form>
